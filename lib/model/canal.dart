import 'package:flutter/material.dart';

class Canal {
  String nomeCanal;
  AssetImage perfilImagem;

  Canal(String nomeCanal, AssetImage perfilImagem) {
    this.nomeCanal = nomeCanal;
    this.perfilImagem = perfilImagem;
  }
}
